export interface GameStats {
  health: number;    // 체력 (0~100)
  fatigue: number;   // 피로도 (0~100)
  stress: number;    // 스트레스 (0~100)
  knowledge: number; // 지능 (0~100)
  happiness: number; // 행복도 (0~100)
}
